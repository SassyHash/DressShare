 <% if object.errors.any? %>
   <div class="alert alert-error">
     The form contains <%= pluralize(object.errors.count, "error") %>.
     <% object.errors.each do |err, msg| %>
      <%= object%><%= msg %>
     <% end %>
   </div>
<% end %>